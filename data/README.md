# This directory stores the scraped data.
# After running scrape_classes.py you will find:
#   classes.json      — the full merged class repository
#   raw_index.json    — the raw name→URL index from the wiki
